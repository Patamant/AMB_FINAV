<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AMB · Tablero financiero · Julio 2026</title>
<style>
  :root{
    --bg:#0F1720;
    --surface:#161F2A;
    --surface-alt:#1C2733;
    --border:#2A3847;
    --text:#ECEEF1;
    --muted:#93A1B0;
    --teal:#3AA890;
    --teal-dim:#2A5E52;
    --amber:#DB9A4C;
    --red:#C15C52;
    --blue:#5F86AA;
    --font-serif: Georgia, 'Iowan Old Style', 'Palatino Linotype', 'Book Antiqua', serif;
    --font-sans: -apple-system, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
  }
  *{box-sizing:border-box;}
  body{
    margin:0;
    background:var(--bg);
    color:var(--text);
    font-family:var(--font-sans);
    line-height:1.5;
  }
  .wrap{max-width:1180px;margin:0 auto;padding:0 24px 64px;}

  /* Header */
  header{
    padding:40px 24px 32px;
    border-bottom:1px solid var(--border);
    margin-bottom:32px;
  }
  header .inner{max-width:1180px;margin:0 auto;display:flex;justify-content:space-between;align-items:flex-end;flex-wrap:wrap;gap:16px;}
  .brand{font-family:var(--font-serif);font-size:28px;letter-spacing:0.2px;}
  .brand span{color:var(--muted);font-family:var(--font-sans);font-size:14px;display:block;margin-top:4px;font-weight:400;}
  .period{
    text-align:right;
    font-size:14px;
    color:var(--muted);
  }
  .period strong{color:var(--text);font-size:16px;display:block;font-family:var(--font-serif);}

  /* KPI strip */
  .kpis{
    display:grid;
    grid-template-columns:repeat(4,1fr);
    gap:1px;
    background:var(--border);
    border:1px solid var(--border);
    border-radius:10px;
    overflow:hidden;
    margin-bottom:36px;
  }
  .kpi{background:var(--surface);padding:22px 20px;}
  .kpi .label{font-size:12.5px;color:var(--muted);margin-bottom:10px;}
  .kpi .value{font-family:var(--font-serif);font-size:30px;line-height:1.1;}
  .kpi .sub{font-size:12.5px;color:var(--muted);margin-top:8px;}
  .kpi .value.teal{color:var(--teal);}
  .kpi .value.amber{color:var(--amber);}

  /* Section shell */
  section.card{
    background:var(--surface);
    border:1px solid var(--border);
    border-radius:10px;
    padding:26px 28px 28px;
    margin-bottom:22px;
  }
  .row-2{display:grid;grid-template-columns:1fr 1fr;gap:22px;}
  .card h2{
    font-family:var(--font-serif);
    font-weight:400;
    font-size:19px;
    margin:0 0 4px;
  }
  .card .desc{font-size:13.5px;color:var(--muted);margin:0 0 20px;max-width:60ch;}

  /* Waterfall */
  .waterfall{display:flex;flex-direction:column;gap:14px;margin-top:6px;}
  .wf-row{display:grid;grid-template-columns:170px 1fr 120px;align-items:center;gap:14px;}
  .wf-label{font-size:13.5px;color:var(--muted);}
  .wf-label.strong{color:var(--text);font-weight:600;}
  .wf-track{position:relative;height:20px;background:var(--surface-alt);border-radius:4px;overflow:hidden;}
  .wf-bar{
    position:absolute;top:0;bottom:0;border-radius:4px;
    width:0;
    transition:width 1.1s cubic-bezier(.2,.7,.3,1);
  }
  .wf-bar.pos{background:var(--teal);}
  .wf-bar.neg{background:var(--red);}
  .wf-bar.sub{background:var(--blue);}
  .wf-bar.final{background:var(--teal);}
  .wf-value{font-size:13.5px;text-align:right;font-variant-numeric:tabular-nums;}
  .wf-value.neg{color:var(--red);}
  .wf-value.pos{color:var(--teal);}

  /* Donut */
  .donut-wrap{display:flex;align-items:center;gap:28px;flex-wrap:wrap;}
  .donut{
    width:168px;height:168px;border-radius:50%;
    flex:none;
    position:relative;
  }
  .donut::after{
    content:"";
    position:absolute;inset:22px;
    background:var(--surface);
    border-radius:50%;
  }
  .donut-center{
    position:absolute;inset:0;display:flex;align-items:center;justify-content:center;flex-direction:column;
    font-family:var(--font-serif);
  }
  .donut-center b{font-size:22px;}
  .donut-center span{font-size:11px;color:var(--muted);font-family:var(--font-sans);}
  .legend{display:flex;flex-direction:column;gap:10px;flex:1;min-width:180px;}
  .legend-item{display:flex;align-items:center;gap:10px;font-size:13.5px;}
  .swatch{width:10px;height:10px;border-radius:2px;flex:none;}
  .legend-item .name{flex:1;color:var(--text);}
  .legend-item .pct{color:var(--muted);font-variant-numeric:tabular-nums;width:46px;text-align:right;}
  .legend-item .amt{color:var(--muted);font-variant-numeric:tabular-nums;width:92px;text-align:right;}

  /* Stacked bar */
  .stack{height:34px;border-radius:6px;overflow:hidden;display:flex;background:var(--surface-alt);margin-bottom:16px;}
  .stack-seg{height:100%;}
  .stack-legend{display:flex;flex-wrap:wrap;gap:16px 22px;}

  /* Gauges */
  .gauges{display:flex;flex-direction:column;gap:22px;}
  .gauge-row{display:grid;grid-template-columns:190px 1fr 90px;align-items:center;gap:16px;}
  .gauge-label{font-size:13.5px;}
  .gauge-label small{display:block;color:var(--muted);font-size:12px;margin-top:2px;}
  .gauge-track{position:relative;height:14px;background:var(--surface-alt);border-radius:7px;}
  .gauge-fill{position:absolute;top:0;left:0;bottom:0;border-radius:7px;width:0;transition:width 1.1s cubic-bezier(.2,.7,.3,1);}
  .gauge-mark{position:absolute;top:-4px;bottom:-4px;width:2px;background:var(--muted);opacity:0.6;}
  .gauge-mark::after{content:attr(data-label);position:absolute;top:-18px;left:50%;transform:translateX(-50%);font-size:10.5px;color:var(--muted);white-space:nowrap;}
  .gauge-value{font-size:15px;text-align:right;font-family:var(--font-serif);}

  /* Badge */
  .badge{
    display:inline-flex;align-items:center;gap:8px;
    background:rgba(58,168,144,0.12);
    border:1px solid var(--teal-dim);
    color:var(--teal);
    padding:8px 14px;border-radius:100px;
    font-size:13px;
  }
  .badge-dot{width:7px;height:7px;border-radius:50%;background:var(--teal);}

  /* Lists */
  .flag-list{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:16px;}
  .flag-list li{
    padding-left:16px;border-left:3px solid var(--amber);
    font-size:13.8px;color:var(--text);
  }
  .flag-list li.high{border-left-color:var(--red);}
  .flag-list li b{display:block;font-size:14.5px;margin-bottom:3px;}
  .flag-list li span.tag{
    display:inline-block;font-size:10.5px;color:var(--muted);margin-top:6px;
  }

  .commit-list{list-style:none;margin:0;padding:0;counter-reset:c;display:flex;flex-direction:column;gap:16px;}
  .commit-list li{
    counter-increment:c;
    display:grid;grid-template-columns:28px 1fr;gap:12px;
    font-size:13.8px;
  }
  .commit-list li::before{
    content:counter(c);
    font-family:var(--font-serif);
    color:var(--blue);
    font-size:16px;
    line-height:1.4;
  }
  .commit-list li b{display:block;font-size:14.3px;margin-bottom:2px;}
  .commit-list li span.owner{color:var(--muted);font-size:12.5px;}

  footer{
    text-align:center;color:var(--muted);font-size:12px;padding-top:12px;
  }

  @media (max-width: 820px){
    .row-2{grid-template-columns:1fr;}
    .kpis{grid-template-columns:1fr 1fr;}
    .wf-row{grid-template-columns:110px 1fr 90px;}
    .gauge-row{grid-template-columns:130px 1fr 70px;}
    header .inner{flex-direction:column;align-items:flex-start;}
    .period{text-align:left;}
  }
</style>
</head>
<body>

<header>
  <div class="inner">
    <div class="brand">Aguiar Morales y Asociados S.A.
      <span>Agencia Asesora Productora de Seguros — tablero de lectura financiera</span>
    </div>
    <div class="period">
      Estados financieros acumulados
      <strong>Enero – Julio 2026</strong>
    </div>
  </div>
</header>

<div class="wrap">

  <!-- KPI strip -->
  <div class="kpis">
    <div class="kpi">
      <div class="label">Ingresos acumulados</div>
      <div class="value">$473.8K</div>
      <div class="sub">99.7% de actividades ordinarias</div>
    </div>
    <div class="kpi">
      <div class="label">Utilidad neta</div>
      <div class="value teal">$50.2K</div>
      <div class="sub">Coincide con Estado de Resultados</div>
    </div>
    <div class="kpi">
      <div class="label">Margen operativo</div>
      <div class="value">14.3%</div>
      <div class="sub">Antes de partidas no operacionales</div>
    </div>
    <div class="kpi">
      <div class="label">Margen neto</div>
      <div class="value">10.6%</div>
      <div class="sub">Sobre ingresos totales</div>
    </div>
  </div>

  <!-- Waterfall -->
  <section class="card">
    <h2>De ingresos a utilidad: la cascada del resultado</h2>
    <p class="desc">Cada barra representa una partida del Estado de Resultados, a escala de dólares reales. Las barras azules son subtotales de control.</p>
    <div class="waterfall" id="waterfall">
      <div class="wf-row">
        <div class="wf-label strong">Ingresos</div>
        <div class="wf-track"><div class="wf-bar pos" data-left="0" data-width="100"></div></div>
        <div class="wf-value pos">$473,780</div>
      </div>
      <div class="wf-row">
        <div class="wf-label">Gasto de ventas</div>
        <div class="wf-track"><div class="wf-bar neg" data-left="58.75" data-width="41.25"></div></div>
        <div class="wf-value neg">–$195,416</div>
      </div>
      <div class="wf-row">
        <div class="wf-label">Gasto administrativo y financiero</div>
        <div class="wf-track"><div class="wf-bar neg" data-left="14.31" data-width="44.45"></div></div>
        <div class="wf-value neg">–$210,601</div>
      </div>
      <div class="wf-row">
        <div class="wf-label strong">= Utilidad operativa</div>
        <div class="wf-track"><div class="wf-bar sub" data-left="0" data-width="14.31"></div></div>
        <div class="wf-value">$67,764</div>
      </div>
      <div class="wf-row">
        <div class="wf-label">Gastos no operacionales</div>
        <div class="wf-track"><div class="wf-bar neg" data-left="10.60" data-width="3.71"></div></div>
        <div class="wf-value neg">–$17,552</div>
      </div>
      <div class="wf-row">
        <div class="wf-label strong">= Utilidad neta</div>
        <div class="wf-track"><div class="wf-bar final" data-left="0" data-width="10.60"></div></div>
        <div class="wf-value pos">$50,212</div>
      </div>
    </div>
  </section>

  <!-- Row: concentración + asignación -->
  <div class="row-2">
    <section class="card">
      <h2>Concentración de ingresos</h2>
      <p class="desc">El 81.6% de los ingresos ordinarios depende de una sola línea comercial.</p>
      <div class="donut-wrap">
        <div class="donut" style="background:conic-gradient(var(--teal) 0deg 293.8deg, var(--blue) 293.8deg 349.2deg, var(--amber) 349.2deg 360deg);">
          <div class="donut-center"><b>81.6%</b><span>Com Vam</span></div>
        </div>
        <div class="legend">
          <div class="legend-item"><span class="swatch" style="background:var(--teal)"></span><span class="name">Com Vam</span><span class="pct">81.6%</span><span class="amt">$385,224</span></div>
          <div class="legend-item"><span class="swatch" style="background:var(--blue)"></span><span class="name">Com Gen</span><span class="pct">15.4%</span><span class="amt">$72,759</span></div>
          <div class="legend-item"><span class="swatch" style="background:var(--amber)"></span><span class="name">Otros (vida, Cidescol, bonif.)</span><span class="pct">3.0%</span><span class="amt">$14,294</span></div>
        </div>
      </div>
    </section>

    <section class="card">
      <h2>Cómo se reparte cada dólar de ingreso</h2>
      <p class="desc">De cada dólar facturado, 85.7 centavos se destinan a operar el negocio.</p>
      <div class="stack">
        <div class="stack-seg" style="width:41.3%;background:var(--red)"></div>
        <div class="stack-seg" style="width:44.5%;background:var(--amber)"></div>
        <div class="stack-seg" style="width:3.7%;background:#6b5847"></div>
        <div class="stack-seg" style="width:10.6%;background:var(--teal)"></div>
      </div>
      <div class="stack-legend">
        <div class="legend-item"><span class="swatch" style="background:var(--red)"></span><span class="name">Ventas — 41.3¢</span></div>
        <div class="legend-item"><span class="swatch" style="background:var(--amber)"></span><span class="name">Administrativo — 44.5¢</span></div>
        <div class="legend-item"><span class="swatch" style="background:#6b5847"></span><span class="name">No operacional — 3.7¢</span></div>
        <div class="legend-item"><span class="swatch" style="background:var(--teal)"></span><span class="name">Utilidad neta — 10.6¢</span></div>
      </div>
    </section>
  </div>

  <!-- Liquidez -->
  <section class="card">
    <h2>Liquidez: lo que parece frente a lo que hay</h2>
    <p class="desc">El índice corriente luce sólido, pero la caja real disponible es mucho más ajustada de lo que ese número sugiere.</p>
    <div class="gauges">
      <div class="gauge-row">
        <div class="gauge-label">Liquidez corriente<small>Activo corriente / Pasivo corriente</small></div>
        <div class="gauge-track">
          <div class="gauge-mark" style="left:33.3%" data-label="1.0x"></div>
          <div class="gauge-fill" data-width="67" style="background:var(--teal)"></div>
        </div>
        <div class="gauge-value">2.01x</div>
      </div>
      <div class="gauge-row">
        <div class="gauge-label">Liquidez de caja<small>Solo efectivo / Pasivo corriente</small></div>
        <div class="gauge-track">
          <div class="gauge-mark" style="left:33.3%" data-label="1.0x"></div>
          <div class="gauge-fill" data-width="5.7" style="background:var(--red)"></div>
        </div>
        <div class="gauge-value">0.17x</div>
      </div>
    </div>
  </section>

  <!-- Row: composición activo + CxC -->
  <div class="row-2">
    <section class="card">
      <h2>Composición del activo corriente</h2>
      <p class="desc">Solo el 8.6% del activo corriente es efectivo disponible de inmediato.</p>
      <div class="stack">
        <div class="stack-seg" style="width:8.6%;background:var(--teal)"></div>
        <div class="stack-seg" style="width:54.6%;background:var(--blue)"></div>
        <div class="stack-seg" style="width:6.1%;background:#6b5847"></div>
        <div class="stack-seg" style="width:30.7%;background:var(--amber)"></div>
      </div>
      <div class="stack-legend">
        <div class="legend-item"><span class="swatch" style="background:var(--teal)"></span><span class="name">Efectivo — 8.6%</span></div>
        <div class="legend-item"><span class="swatch" style="background:var(--blue)"></span><span class="name">Cuentas y doc. por cobrar — 54.6%</span></div>
        <div class="legend-item"><span class="swatch" style="background:#6b5847"></span><span class="name">Pagos anticipados — 6.1%</span></div>
        <div class="legend-item"><span class="swatch" style="background:var(--amber)"></span><span class="name">Crédito tributario (SRI) — 30.7%</span></div>
      </div>
    </section>

    <section class="card">
      <h2>Cuentas por cobrar: ¿quién debe?</h2>
      <p class="desc">El 91.8% de las cuentas por cobrar son de partes relacionadas, no de clientes.</p>
      <div class="stack">
        <div class="stack-seg" style="width:8.2%;background:var(--teal)"></div>
        <div class="stack-seg" style="width:30.5%;background:var(--amber)"></div>
        <div class="stack-seg" style="width:61.3%;background:var(--red)"></div>
      </div>
      <div class="stack-legend">
        <div class="legend-item"><span class="swatch" style="background:var(--teal)"></span><span class="name">Clientes comerciales — 8.2%</span></div>
        <div class="legend-item"><span class="swatch" style="background:var(--amber)"></span><span class="name">Accionista (Rodrigo Aguiar) — 30.5%</span></div>
        <div class="legend-item"><span class="swatch" style="background:var(--red)"></span><span class="name">Funcionarios/empleados — 61.3%</span></div>
      </div>
    </section>
  </div>

  <!-- Pasivo -->
  <section class="card">
    <h2>Estructura del pasivo</h2>
    <p class="desc">El 90.1% del pasivo es de naturaleza laboral, no financiera.</p>
    <div class="donut-wrap">
      <div class="donut" style="background:conic-gradient(var(--blue) 0deg 324.3deg, var(--amber) 324.3deg 348.8deg, #6b5847 348.8deg 356.1deg, var(--red) 356.1deg 360deg);">
        <div class="donut-center"><b>90.1%</b><span>Laboral</span></div>
      </div>
      <div class="legend">
        <div class="legend-item"><span class="swatch" style="background:var(--blue)"></span><span class="name">Obligaciones laborales (CP+LP)</span><span class="pct">90.1%</span><span class="amt">$123,801</span></div>
        <div class="legend-item"><span class="swatch" style="background:var(--amber)"></span><span class="name">Obligaciones fiscales</span><span class="pct">6.8%</span><span class="amt">$9,355</span></div>
        <div class="legend-item"><span class="swatch" style="background:#6b5847"></span><span class="name">Proveedores</span><span class="pct">2.0%</span><span class="amt">$2,789</span></div>
        <div class="legend-item"><span class="swatch" style="background:var(--red)"></span><span class="name">Diversas/relacionadas</span><span class="pct">1.1%</span><span class="amt">$1,500</span></div>
      </div>
    </div>
    <div style="margin-top:18px;">
      <span class="badge"><span class="badge-dot"></span>Sin deuda con instituciones financieras — US$0 corriente y no corriente</span>
    </div>
  </section>

  <!-- Row: alertas + compromisos -->
  <div class="row-2">
    <section class="card">
      <h2>Puntos de atención</h2>
      <p class="desc">Lo que el Consejo debería preguntar primero.</p>
      <ul class="flag-list">
        <li class="high"><b>Diferencia sin conciliar en el resultado</b>El patrimonio registra $40,068 de resultado del ejercicio frente a $50,212 del Estado de Resultados — brecha de $10,144 pendiente de explicación contable.<span class="tag">Prioridad alta</span></li>
        <li class="high"><b>Caja atrapada en partes relacionadas</b>El 91.8% de las cuentas por cobrar corresponde a un accionista y dos empleadas, no a clientes.<span class="tag">Prioridad alta</span></li>
        <li><b>Concentración comercial</b>81.6% de los ingresos depende de una sola línea ("Com Vam").<span class="tag">Seguimiento</span></li>
        <li><b>Flota depreciada al 53%</b>Vehículos con $90,137 en valor bruto y $47,744 de depreciación acumulada; capex próximo probable.<span class="tag">Seguimiento</span></li>
      </ul>
    </section>

    <section class="card">
      <h2>Compromisos con fecha</h2>
      <p class="desc">Lo que el equipo financiero trae al próximo Consejo.</p>
      <ol class="commit-list">
        <li><b>Conciliar la diferencia de $10,144</b> entre patrimonio y Estado de Resultados.<span class="owner">Contabilidad</span></li>
        <li><b>Cronograma de cobro</b> de las cuentas por cobrar a accionista y empleados.<span class="owner">Tesorería</span></li>
        <li><b>Justificar el contrato "Servicios Vicemi Adm"</b> ($48,920) y su naturaleza.<span class="owner">Administración</span></li>
        <li><b>Plan de renovación de flota</b> con impacto proyectado en caja.<span class="owner">Finanzas / Operaciones</span></li>
      </ol>
    </section>
  </div>

  <footer>
    Elaborado a partir del Estado de Situación Financiera al 31/07/2026 y el Estado de Resultados 01/01/2026–31/07/2026 (Contifico). Cifras acumuladas, sin período comparativo disponible.
  </footer>

</div>

<script>
  // Single orchestrated reveal for the waterfall + gauges on load
  window.addEventListener('DOMContentLoaded', () => {
    requestAnimationFrame(() => {
      document.querySelectorAll('.wf-bar').forEach(el => {
        el.style.left = el.dataset.left + '%';
        el.style.width = el.dataset.width + '%';
      });
      document.querySelectorAll('.gauge-fill').forEach(el => {
        el.style.width = el.dataset.width + '%';
      });
    });
  });
</script>

</body>
</html>
